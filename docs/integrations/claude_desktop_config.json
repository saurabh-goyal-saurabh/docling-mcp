{
    "mcpServers": {
        "docling": {
            "command": "<use output of `which uv` in the `docling-mcp` folder>",
            "args": [
                "--directory",
                "<explicit path to docling-mcp or use output `pwd` in `docling-mcp` folder>",
                "run",
                "docling-mcp-server"
            ]
        }	
    }
}
